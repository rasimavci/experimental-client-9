const credentials = state => state.credentials
const contacts = state => state.contacts
const mystate = state => state
const conversations = state => state.conversations

export { credentials, contacts, mystate, conversations }
